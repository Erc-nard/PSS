#include <stdio.h>

int pick(int n,int picked[],int m, int toPick){
    if(toPick==0){
        return;
    }

    if(m==toPick)
        smallest=0;
    else
        smallest=;

    for(i=smallest;i<n;i++){

    }
}
int main(){
    int picked[4];
    pick(7,4,picked,4);
}